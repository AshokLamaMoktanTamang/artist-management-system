# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build artist-management-system-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t artist-management-system-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system artist-management-system-api && \
          adduser --system -G artist-management-system-api artist-management-system-api

COPY apps/artist-management-system-api/dist artist-management-system-api/
RUN chown -R artist-management-system-api:artist-management-system-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix artist-management-system-api --omit=dev -f install

CMD [ "node", "artist-management-system-api" ]
